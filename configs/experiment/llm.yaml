# @package _global_

defaults:
  - override /trainer: gpu
  - override /data: gigaword
  - override /callbacks: dummy_callbacks
  - override /model: llm
  - override /logger: default

project_name: 'andreas-test-llm'
task_name: 'andreas-test'
compile: False # we can only compile on GPU
phase: 'train'

data:
  batch_size: 1

logger:
  log_model: False

callbacks:
  text_callback:
    _target_: src.callbacks.TextCallback
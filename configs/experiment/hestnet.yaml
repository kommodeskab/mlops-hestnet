# @package _global_

defaults:
  - override /trainer: cpu
  - override /data: gigaword_data
  - override /callbacks: dummy_callbacks
  - override /model: hestnet
  - override /logger: default

project_name: 'hestnet_experiment'
task_name: 'hestnet_task'
compile: False # we can only compile on GPU
phase: 'train'
checkpoint: distilbert/distilgpt2

data:
  batch_size: 2

trainer:
  log_every_n_steps: 100
  max_epochs: 3
